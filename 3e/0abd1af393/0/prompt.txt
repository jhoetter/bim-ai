[Image #3] that is now the current state; i can't click on e.g. the project menu to seed the initial house, i cant draw walls, the canvas is not fully heighted, it is dark, [Image #4] the navigation cubicle in 3D isnt really a cubicle (3d design, revit really has that i see the perspective that way etc), ... -> still A LOT to improve. please update the ui spec file by first analyzing ANYTHING we need to do, then coninue. also, borrow the conservative.css from the ~/repos/hof-os and copy-paste it to our repo so we can test how it would look with an applied design-system please

---

also, i notice that revit is "tab-based", i.e. i can create many tabs for the different things i want, e.g. 2d, 3d, ... and it is not as we currently have it. see revit: [Image #5] so please also add that..

---

which open tasks are there? write them all down. are they documented somewhere?

---

write them really into some md so it is EASY to find them directly, that they don't get lost. then commit + push all

---

ok, please now continue working on all the open topics we have left; create a plan into separate workpackages, commit and push after each is done. don't stop until fully done. if you notice that certain parts also need the backend functionality that isnt impelmented yet, go ahead and do that as well directly (i.e. full-stack); if the backend already exists, perfect (check the APIs etc).