Continue BIM AI UX implementation in /Users/jhoetter/repos/bim-ai, but use the prepared WP worktree /tmp/bim-ai-wp44-picked for the current unfinished slice.

  Current workpackage:
  WP-NEXT-44 — Generate Walls From Floors, Rooms, And Picked Boundaries.

  Important current state:
  - Previous WP-NEXT-44 floor/room boundary wall generation was already committed and pushed as 9205ed6cb “Add boundary wall generation preview”.
  - The current uncommitted slice in /tmp/bim-ai-wp44-picked adds active Wall-tool picked-line authoring for plan:
    - packages/web/src/plan/wallPickLines.ts
    - packages/web/src/plan/wallPickLines.test.ts
    - packages/web/src/plan/PlanCanvas.tsx
    - packages/web/src/plan/PlanCanvas.toolDestubs.test.ts
    - packages/web/src/tools/authoringCommandContract.ts
    - packages/web/tmp/ux-next-wp44-picklines-20260515/capture.mjs
  - Do not carry over /tmp/bim-ai-wp42 Viewport.tsx changes; that was obsolete conflict work. /tmp/bim-ai-wp44-picked is based on current origin/main.
  1. Ensure ports 2000/8500 serve /tmp/bim-ai-wp44-picked, not /Users/jhoetter/repos/bim-ai:
     lsof -tiTCP:2000 -sTCP:LISTEN | xargs -r ps -o pid=,ppid=,command= -p
     lsof -tiTCP:8500 -sTCP:LISTEN | xargs -r ps -o pid=,ppid=,command= -p
  2. Then:
     make seed name=target-house-3
     make dev name=target-house-3

  Validation target:
  - http://127.0.0.1:2000/
  - http://127.0.0.1:8500/

  Known issue:
  - The package test command `pnpm --filter @bim-ai/web test -- ...` currently runs the whole `src` suite and fails an unrelated current-main test:
    `src/workspace/project/ProjectSetupDialog.test.tsx > creates location sun settings when missing`
    because recent GeoMapPicker changes removed/changed the visible `Latitude deg`/`Longitude deg` labels and Leaflet throws in jsdom.
  - Focused direct vitest passed:
    `pnpm --filter @bim-ai/web exec vitest run src/plan/wallPickLines.test.ts src/plan/PlanCanvas.toolDestubs.test.ts src/tools/authoringCommandContract.test.ts src/workspace/commandCapabilities.test.ts`
  - Typecheck passed:
    `pnpm --filter @bim-ai/web typecheck`

  Next concrete steps:
  1. Fix `packages/web/tmp/ux-next-wp44-picklines-20260515/capture.mjs`.
     It currently fails waiting for `[data-testid="wall-pick-line-preview"]` in the floor-edge proof after command hydration. Debug PlanCanvas hit-testing / level selection / sampled screen point.
  2. Capture valid screenshots:
     - 01-wall-tool-pick-floor-edge-preview.png
     - 02-wall-tool-pick-floor-edge-created.png
     - 03-wall-tool-pick-floor-edge-duplicate-blocked.png
     - 04-wall-tool-pick-dxf-line-preview.png
     - 05-wall-tool-pick-dxf-line-created.png
     - summary.json
  3. Confirm summary:
     - floorEdgeWallCreated: true
     - dxfLineWallCreated: true
     - duplicateNotice contains “Existing wall already overlaps”
     - activeTool: "wall"
     - noMainFrameNavigationsAfterBoot: true
     - no console/page errors except harmless favicon if any
  4. Run:
     pnpm --filter @bim-ai/web typecheck
     pnpm --filter @bim-ai/web exec vitest run src/plan/wallPickLines.test.ts src/plan/PlanCanvas.toolDestubs.test.ts src/workspace/commandCapabilities.test.ts
     pnpm exec prettier --check packages/web/src/plan/PlanCanvas.tsx packages/web/src/plan/PlanCanvas.toolDestubs.test.ts packages/web/src/plan/wallPickLines.ts packages/web/src/plan/wallPickLines.test.ts
  packages/web/src/tools/authoringCommandContract.ts packages/web/tmp/ux-next-wp44-picklines-20260515/capture.mjs spec/ux-bim-ai-next-tracker.md
     git diff --check
  5. Update `spec/ux-bim-ai-next-tracker.md`.
     Be conservative: keep WP-NEXT-44 Partial unless explicit top-constraint/exterior-side controls are fully covered; otherwise mark Done only if acceptance is proven by tests + seeded screenshots.
  6. Commit:
     git add <intended files only>
     git commit -m "Add wall picked-boundary placement"
     git push origin HEAD:main
  7. After WP-NEXT-44 is closed or explicitly left Partial with evidence, continue with the next tracker workpackage.