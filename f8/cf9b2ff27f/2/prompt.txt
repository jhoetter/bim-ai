Agent D — R2-04 (stair treads)
  Work in /Users/jhoetter/repos/bim-ai. Read spec/rendering-v2-spec.md
  section R2-04. Replace makeStairVolumeMesh in packages/web/src/Viewport.tsx
  with a THREE.Group of per-tread BoxGeometry slabs (40 mm thick) stepped
  from base to top level elevation, plus two side stringer plates. Derive
  tread count from stair.riserCount or Math.round(totalRise / riserMm).

  Only touch makeStairVolumeMesh. Do not touch any other factory functions.
  Run pnpm --filter web typecheck before committing.
  Commit: "feat(3d): R2-04 — stair tread + stringer geometry"
  Push to origin/main when done.

  Task: R2-04