ok, i just pushed a (hopefully) fix for the CI of js. please listen to it, and once you get a result: if the pipeline works, great, continue with the code quality improvements. if not, make changes to according to the error log of the pipeline. continue until all improvements are done, i.e. the CI is workign fully AND the code quality todos are solved. this might take the whole night, but keep working on it..
do a commit for each workpackage, and ultimately merge into main. make sure that everything is merged into main, and no more outstanding remote branches exist at the end of your work.