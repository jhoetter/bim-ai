Agent A — R2-07 + R2-08 (floor polygon + site fixes, combine since they're tiny)
  Work in /Users/jhoetter/repos/bim-ai. Read spec/rendering-v2-spec.md
  sections R2-07 and R2-08. Implement both.

  R2-07: Replace makeFloorSlabMesh in packages/web/src/Viewport.tsx — swap
  the xzBoundsMm BoxGeometry with THREE.Shape + ExtrudeGeometry from
  floor.boundaryMm. Rotate -π/2 so footprint is in XZ. Find slab_opening
  elements with hostFloorId === floor.id and add as THREE.Path holes.
  Call addEdges(mesh, 20) and set castShadow = receiveShadow = true.

  R2-08: In makeSiteMesh, fix the operator precedence bug:
  (site.padThicknessMm ?? 150) / 1000 (not site.padThicknessMm ?? 150/1000).
  Confirm receiveShadow = true, castShadow = false. Add aoMapIntensity: 0
  to the site material.

  Only touch makeFloorSlabMesh and makeSiteMesh. Do not touch any other
  factory functions, the renderer init, or the lighting setup.
  Run pnpm --filter web typecheck before committing.
  Commit: "feat(3d): R2-07/08 — floor polygon extrude + site slab fixes"
  Push to origin/main when done.

  Tasks: R2-07 + R2-08