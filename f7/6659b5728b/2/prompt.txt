so everything is on main, clean state, no more other remote branches or such? tracker fully up-to-date?

---

ok, can you schedule the next wave 4, which is supposed to solve on the one side the leftover WIP wave3-2, but also the next packages?

---

i want all, but why wave-7? wouldnt it be wave 4 then? anyways, schedule as much as possible, that sounds good.

---

so what would be the message i can give an agent to kick off?

---

can you do that the mentioned wave item prompt name is at the end so i dont need to move the cursor so much?

---

ok A and C are kicked off, B will then follow later. where are we currently in the workpackage tracker?

---

C-01 said this:

  Branch: feat/wave-04-edt-05-snap-extra (pushed; commit 356b7dab)

  What shipped (EDT-05 closeout):
  - produceTangentSnaps, produceParallelSnaps, produceWorkplaneSnaps in packages/web/src/plan/snapEngine.ts, plus closestPointOnPolyline helper. All wired into snapPlanCandidates via new
   curves / workplane / draftAnchor opts.
  - SnapKind union extended with parallel and workplane; tangent's reserved-only docstring removed. New rank order: endpoint > intersection > perpendicular > extension > parallel >
  tangent > workplane > grid > raw.
  - snapSettings.ts: SNAP_KINDS exported; new toggles default parallel: true, tangent: false (most expensive producer), workplane: true; existing localStorage key preserved;
  applySnapSettings filters new kinds.
  - SnapGlyphLayer.tsx: distinct glyphs for tangent (circle-tangent), parallel (‖), workplane (skewed wireframe square). SnapSettingsToolbar.tsx exposes the new toggles.
  - snapTabCycle.ts exports canonical SNAP_TAB_CYCLE_ORDER.
  - Tests: test_tangent_snap_on_curved_segment, test_parallel_snap_aligns_to_hovered_wall_direction, test_workplane_snap_projects_pointer_onto_active_plane, plus wiring + per-kind-toggle
   coverage. 45/45 passing.
  - Tracker EDT-05 row flipped partial → done.

  Deviation noted in commit body: Fixed pre-existing TS2783 in grip3d.test.ts:174 (kind: 'wall' was overwritten by ...baseWall spread) — required to make tsc --noEmit pass on main.
  Semantics unchanged.

  Still deferred: Nothing from this WP — closeout is complete. Untracked grip3dProviders.* / grip3dRenderer.* files in the workspace are unrelated WIP from another branch and were left
  alone (not committed to this branch).

  Validation:
  - cd packages/web && pnpm typecheck — clean
  - cd packages/web && pnpm exec vitest run src/plan/snapEngine.test.ts src/plan/snapTabCycle.test.ts src/plan/snapSettings.test.ts — 45/45 passing
 and C-02 said this:
  Branch: feat/wave-04-skt-01-sketch-modes (pushed to origin, commit 23188170)

  What shipped:
  - SUBMODES registry in app/bim_ai/sketch_session.py mapping each of the 7 element kinds (floor, roof, room_separation, ceiling, in_place_mass, void_cut, detail_region) to a (validator,
   Finish-emitter) pair, plus a single finish_session(session, options) dispatch entry point.
  - New commands CreateMassCmd and CreateVoidCutCmd in commands.py, with engine handlers that materialise a MassElem(phase_id='massing') and a marker VoidCutElem co-authored with an
  AgentDeviationElem for traceability.
  - New VoidCutElem in elements.py.
  - New assert_closed_loop and assert_line_set helpers in sketch_validation.py (raising a new SketchInvalidError with shared code strings).
  - SketchSession gains options: dict[str, Any] for sub-mode-specific config; HTTP route accepts a generic options bag at open and finish time.
  - _KIND_TO_NEW_ID_FIELD extended so the route returns the right kind-specific id field for each new sub-mode.
  - 8 new tests in app/tests/test_sketch_session_remaining_modes.py (happy + rejection per sub-mode).
  - Tracker row for SKT-01 flipped partial → done.

  Validation results:
  - ruff check is clean for every file I touched (6 src files + new test file). The wider ruff check bim_ai tests reports 62 pre-existing errors in unrelated skb/* files (down from 64 on
   c931bd16 — net reduction, none introduced).
  - pytest tests/test_sketch_session_remaining_modes.py tests/test_sketch_session.py tests/test_sketch_validation.py → 19 passed.
  - Full pytest tests (deselecting the pre-existing unrelated test_advisor_blocking_class_expansion failure) → 1,584 passed / 12 skipped.

  Deferred (per prompt non-goals): No UI canvas changes; void-cut CSG geometry on the renderer remains scoped out (marker only); no edit-existing-sketch round-trip.

  Operational note: The repo's main worktree was being rapidly checkout-thrashed by parallel batch-A/B agents, so I built and committed this WP from an isolated git worktree at
  /tmp/wave-04-skt-01 to avoid contaminating their WIP — and restored the unrelated uncommitted state I had stashed on the main worktree before exiting.


are they both done, or should i instruct them something?

---

can you do the merge here in the CLI? into main?

---

so can i kick off batch B?

---

agent said it is merged, can you validate it is clean on main? i.e. the batch A

---

all seem done, is that correct? can you ensure we have a clean state, no more local worktrees and such, all on main, all tracked?

---

can you clean up further? does that make sense?

---

can you clean up further? does that make sense? or is it perfectly clean? i mean the local branches of claude, worktrees, etc.

---

go ahead with the cleanup. anything that is wip for workpackages of features, should be in @spec/workpackage-master-tracker.md . but if all good, just go ahead

---

and all committed + pushed?

---

what is left to do in the @spec/workpackage-master-tracker.md

---

can you write the prompts then for wave 5? in how many parallel agents make sense; if all should be sequential, then also good

---

can you write the agent of FED-04 to also pick of FED-03 once done? so that i can kick off all in parallel and once all are done, it can be merged?