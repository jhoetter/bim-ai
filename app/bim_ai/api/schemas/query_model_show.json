{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ModelShowInput",
  "description": "Input schema for the model-show query command.",
  "type": "object",
  "required": ["modelId"],
  "properties": {
    "modelId": {
      "type": "string",
      "format": "uuid",
      "description": "UUID of the model to fetch."
    }
  },
  "additionalProperties": false
}
