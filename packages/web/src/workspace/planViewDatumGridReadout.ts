/** Plan grid / datum inspector line for active plan view (optional UX slice). */

export function buildPlanGridDatumInspectorLine(
  _elementsById: Record<string, unknown>,
  _planProjectionPrimitives: unknown,
  _planViewId: string,
): string {
  return '';
}
