You are not alone in this codebase. Do not revert edits made by others, and do not touch unrelated dirty files unless they directly block this task.

Task: clear the current generated seedhouse and recreate the target house from scratch according to the target-house spec.

Context and intent:
- The user wants the generated seedhouse cleared and rebuilt, not replayed as the old/default house.
- Preserve the spec file(s). Do NOT delete `spec/target-house-seed.md` or other specs.
- Use the sketch-to-BIM methodology now present in the repo. Read `claude-skills/sketch-to-bim/SKILL.md` and `spec/sketch-to-bim-methodology.md` before editing.
- Source of truth is the target-house spec. The user said `spec/target-house`; likely this means `spec/target-house-seed.md` and any adjacent target-house image/reference files. Inspect `spec/` to confirm.
- Use the live app/advisor/screenshot loop where feasible. If a dev server is already running, reuse it. If not, start it only if needed and report the URL.
- Use the CLI evidence tooling (`initiation-check`, `initiation-run`, visual/acceptance gates, golden suite) as applicable.

Ownership / likely write scope:
- Seed source and generation files related to the target seedhouse, e.g. `packages/cli/lib/one-family-home-commands.mjs`, seed DSL/spec examples if needed, scripts or fixtures generated by the seed workflow.
- Generated seed artifacts/snapshots that are intentionally created by the seed workflow.
- Evidence artifacts under `nightshift/` if needed.
- Do not edit unrelated app/web family placement or revit parity files.

Requirements:
1. Determine how `make seed` currently creates/persists the seedhouse, and clear the currently generated seedhouse state while preserving specs.
2. Recreate the target house from scratch according to the target-house spec/image, not by reusing the old seedhouse layout.
3. Ensure the model captures the key visible target features: modern two-storey house, smaller ground base, dominant white upper wrapper, recessed front loggia, asymmetric/gable roof condition, embedded roof terrace/cutout with access/guard, credible interior layout/stairs/rooms, no obvious toy scale, no generic default house.
4. Use advisor findings and screenshots as feedback. Fix warnings where possible; if a blocker remains, document it honestly with element ids and rationale.
5. Run focused tests/evidence commands that prove the seed was rebuilt and can be checked.
6. Commit your changes locally only if the resulting seed/evidence is coherent and tests pass. Do not push unless explicitly asked by the parent/user.

Final response back to me:
- Summarize what you changed and why.
- List changed files.
- Include evidence commands run and their results.
- Include any remaining advisor/visual issues, with exact artifact paths.
- Include commit hash if you committed.

---

Update from parent: current `main` has the target-house inputs under `spec/target-house/`, not the old top-level path. Relevant files:
- `spec/target-house/target-house-seed.md`
- `spec/target-house/target-house-1.png`
- `spec/target-house/target-house-2.png`
- `spec/target-house/floorplan.png`

Preserve this spec directory. Use these files as source of truth. The root `spec/target-house.jpeg` was removed/emptied in the latest commit, so do not depend on it.

---

Please stop the current work and report status immediately. Include: what you completed, current blocker if any, changed files, tests/evidence run, whether you committed, and any artifact paths. Do not continue editing.