#!/bin/sh
# Entire CLI hooks
# Pre-push hook: push session logs alongside user's push
# $1 is the remote name (e.g., "origin")
if [ "$BIM_AI_ENABLE_ENTIRE_GIT_HOOKS" = "1" ]; then
  entire hooks git pre-push "$1" || true
fi
