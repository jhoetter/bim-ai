wave 2 agent 8 is also done, can you check everything is on main? and updated in the tracker?
this is the summary agent 8 gave:
  Summary

  Three WPs shipped on wave2-8 (rebased on top of origin/main):

  VIE-02 — Per-detail-level visibility on family geometry (ae5dd03e, done)
  - visibilityByDetailLevel field on sweep / family_instance_ref / array nodes
  - Resolver + 3D viewport panel resolver thread detailLevel
  - Family editor properties panel — 3-checkbox row per selected sweep
  - 11 new tests (7 resolver + 4 UI), 145 family/family-editor tests still pass

  IFC-03 — Roof-hosted void replay (0bbc3dbd, done)
  - New RoofOpeningElem + CreateRoofOpeningCmd parallel to slab opening
  - Engine handler + authoritative-replay preflight + known_roofs tracking
  - Export emits IfcOpeningElement on IfcRoof; re-parser turns it into createRoofOpening (replacing the roof_host_not_supported_v0 skip)
  - Round-trip: inspect_kernel_ifc_semantics.openingsByHostKind + roofHostedOpenings summary check
  - 9 new tests; all 36 existing IFC tests still pass

  IFC-04 — Materials + composites + broader QTO + classifications (e545733c, partial)
  - Pset_MaterialCommon (Reference / BaseColor / Roughness / Metalness / DisplayName / Category) on every layered IfcMaterial
  - New try_attach_kernel_ifc_single_material for non-layered walls/roofs/doors/windows
  - Wall QTO adds GrossSideArea + NetSideArea; roofs get Qto_SlabBaseQuantities; doors/windows add Area
  - Optional ifcClassificationCode on wall/floor/roof/door/window/room → IfcClassificationReference via shared cache
  - 8 new tests; full python suite (1315 passed) green

  nightshift/wave2-8-status.md and tracker updated; branch is rebased clean and not pushed (waiting on user to merge).

tell me if all is good and i can start wave 3

---

how many agents are in wave 3?

---

except for w3-8 all is done (w3-8 also in the last phases), can you check all is available and then merge into main?

---

w3-8 also finsihed; can you do the EDT-02 and EDT-03 as you suggested? also, can you please look into the @spec/workpackage-master-tracker.md and see what we have for the @spec/target-house-seed.md as the goal to develop? i think it is related to some claude-skill more or less. i want that you yourself think about a sequence plan and then implement all workpackages in this session, always for each workpackage create a commit and push and into main; i think it is likely these:
id,name,category,state,effort,role,bucket
  KRN-04,wall_opening element kind,kernel,done,—,kernel-primitive,essential
  KRN-06,project_base_point + survey_point + internal_origin,kernel,done,—,origin-scaffolding,essential
  KRN-11,Asymmetric gable roof (ridge offset, per-side eave heights),kernel,done (a03da35f),M,kernel-primitive — the dramatic asymmetric silhouette,essential
  KRN-12,Variable-shape window outline (trapezoidal/arched/gable-end/custom),kernel,done (99f4c8ef + 1d77d4f4),M,kernel-primitive — loggia trapezoidal window,essential
  KRN-13,Non-swing door operation types (sliding/pocket/bi-fold/pivot/auto),kernel,done (2dc556dd),S,kernel-primitive — sliding glass loggia + dormer doors,essential
  KRN-14,Dormer element kind (cuts host roof; adds walls + roof),kernel,partial (seed-fidelity sprint — load-bearing slice + CSG cut shipped; gable/hipped roof kinds + hasFloorOpening +
  plan symbology deferred),L,kernel-primitive — east-slope dormer cut-out,essential
  KRN-15,createSweep engine command (lift FAM-02 to project authoring),kernel,done (seed-fidelity sprint),M,kernel-primitive — picture-frame outline along gable polygon,essential
  KRN-16,Wall recess / setback geometry (loggias / bay windows / deep porches),kernel,done (seed-fidelity sprint),M,kernel-primitive — loggia recess on south facade,essential
  FAM-02,Sweep family geometry node,family-system,done,—,renderer-helper — used by KRN-15,essential
  MAT-01,Material catalog enrichment + standing-seam metal roof,materials,done (9231887b),M,materials — every catalog key the target house cites,essential
  SKB-03,Visual checkpoint tool (deterministic screenshot + delta vs target),sketch-to-bim,open,L,methodology — agent's eyes (priority bundle),essential
  SKB-04,Sketch-to-dimensions calibrator,sketch-to-bim,open,M,methodology — pixel-to-mm anchoring (priority bundle),essential
  SKB-05,Architectural soundness validator pack,sketch-to-bim,open,M,validator — corners/floors/levels/roof footprint (priority bundle),essential
  SKB-10,Per-phase visual-verification gate,sketch-to-bim,open,M,methodology — wires SKB-03 into try_commit_bundle (priority bundle),essential
  SKB-11,Roof-wall alignment validator,sketch-to-bim,open,S,validator — roof footprint contains every upper-floor wall centerline (priority bundle),essential
  SKB-12,Architect cookbook (sketch-to-BIM methodology skill),sketch-to-bim,done (this thread; claude-skills/sketch-to-bim/SKILL.md),S,methodology — the skill agents load (priority
  bundle),essential
  SKB-15,Recursive refine loop (formal agent workflow),sketch-to-bim,open,M,methodology — within-phase iteration with cap (priority bundle),essential
  SKB-21,Sketch-to-BIM brief format (canonical input),sketch-to-bim,open,S,methodology — structured sketch+brief input the agent fills out (priority bundle),essential
  SKB-01,Phased build workflow + phase tag on commits,sketch-to-bim,open,M,methodology — replaces atomic 87-cmd bundle with 5-7 phase bundles,supporting
  SKB-02,mass element kind (volumetric massing primitive),sketch-to-bim,open,L,methodology — primitive blocks before walls,supporting
  SKB-06,Proportion / scale linter (architectural_proportions_v1),sketch-to-bim,open,S,validator — advisory thresholds for room/wall/door/window/roof,supporting
  SKB-07,Region colour sampler → MAT-01 nearest-match,sketch-to-bim,open,S,deterministic vision aid — RGB sampler returning top-3 catalog matches,supporting
  SKB-08,Phase-checkpoint assumption log,sketch-to-bim,open,S,methodology — phaseId + sketchAnchorMm on agent_assumption,supporting
  SKB-09,Architectural archetype library (starter bundles),sketch-to-bim,open,L,methodology — fork from closest archetype rather than blank canvas,supporting
  SKB-13,Element-count priors per archetype,sketch-to-bim,open,XS,methodology — sanity bound on element counts per house type,supporting
  SKB-14,Sketch edge-detection helper (classical CV),sketch-to-bim,open,S,deterministic vision aid — Canny/edges PNG for the agent to read,supporting
  SKB-16,Camera presets matched to sketch perspectives,sketch-to-bim,open,XS,methodology — vp-main-iso / vp-front-elev / etc. matching sketch panels,supporting
  SKB-18,Phase-by-phase test fixtures,sketch-to-bim,open,M,test — regression per phase (deps SKB-01),supporting
  SKB-19,Wall-graph closure validator (stronger VAL-01),sketch-to-bim,open,S,validator — orphan walls / non-90° / T-intersections,supporting
  SKB-20,Architectural style bias vectors,sketch-to-bim,open,S,methodology — modernist/traditional defaults,supporting
  SKB-22,Wall-corner join markers + auto-join,sketch-to-bim,open,M,renderer-quality — coincident endpoints get join_geometry,supporting
  SKB-23,Per-phase preview rendering mode,sketch-to-bim,open,S,viewer — viewerPhaseFilter to see model at end of phase N,supporting

please implement all of them. i am going away for some longer time, so work on them all diligently; commit and push after each, and ofc update the tracker...