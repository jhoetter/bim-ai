You are tasked with recreating the BIM seedhouse from scratch using the repository's sketch-to-BIM methodology. This is intentionally delegated so you are not biased by the current bad seed implementation.

Context and goal:
- Repo: /Users/jhoetter/repos/bim-ai
- The user wants to test whether the improved sketch-to-BIM methodology can generate a correct seedhouse from the sketch/spec.
- Treat this as project initiation, not a decorative preview.
- Read `claude-skills/sketch-to-bim/SKILL.md` first and obey it as a hard operating contract.
- Read `spec/target-house-seed.md` and any referenced/available target-house imagery. If target image files are not available, use the spec and thread context: modern house with smaller vertically-clad ground-floor base, larger smooth-white upper wrapper volume, deep front recessed loggia, black horizontal rails, asymmetric/folded white roof, and a real embedded roof terrace/cutout visible from above/right.

Critical methodology requirements:
- Use the local app/advisor/render feedback loop. Start or reuse `make dev` / localhost:2000 if needed.
- Rebuild from scratch. Do not tweak the existing seed incrementally or use the current room-separation workaround. You may inspect current `packages/cli/lib/one-family-home-commands.mjs` only to understand command APIs and anti-patterns, then replace the model design deliberately.
- Use real partition/wall geometry for rooms. Do not draw room-separation rectangles around every room.
- Advisor warnings are hard evidence. These block acceptance unless explicitly documented with rationale: `room_boundary_open`, `room_unenclosed`, `room_derived_interior_separation_ambiguous`, inappropriate `room_no_door`, `room_target_area_mismatch`, `floor_overlap`, host/opening warnings, stair comfort or shaft mismatch, roof/roof-opening/material warnings, schedule/sheet viewport warnings.
- Capture rendered screenshots from at least: main sketch-matched axonometric, front elevation/perspective, side/rear or roof terrace view, ground plan or transparent/wire diagnostic. Inspect them visually yourself and write a concise visual verdict.
- Use the CLI advisor after `make seed`, e.g. `BIM_AI_MODEL_ID=75cd3d5c-f28c-5dd2-b8bf-8cbba71fd10f node packages/cli/cli.mjs advisor --output json --severity warning`.

Implementation ownership:
- You may edit the seed bundle source (`packages/cli/lib/one-family-home-commands.mjs`) and regenerate `packages/web/e2e/__fixtures__/seed-target-house-snapshot.json`.
- You may add an evidence/status markdown under `nightshift/` if helpful.
- Do not edit unrelated web UI files.
- Do not revert or overwrite unrelated user changes. You are not alone in the codebase.
- Do not commit or push. Leave changes in your forked workspace and report exactly which files you changed.

Verification expected:
- `node scripts/build-seed-snapshot.mjs`
- `make seed`
- CLI advisor warning pass and summary of remaining warnings, if any
- Playwright target-house visual test or SKB checkpoint render, plus inspect actual PNG paths
- Relevant tests if you touch renderer/state code (avoid that if possible)

Final response required:
- Changed files
- Commands run and results
- Advisor findings remaining, with classification (fixed/tolerated/blocking)
- Screenshot paths inspected and visual verdict
- A short, honest answer: did the methodology produce a usable project-initiation model or not? If not, say exactly where it failed.