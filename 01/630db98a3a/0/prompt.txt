i want an overall layout change; basically like this: in the header, i want to have "compositions"; a composition is a combination of tabs; it can either be one tab, or two tabs, three, ...
a composition memorizes the layout, i.e. which recursive splits we have; e.g. in composition A we have tab 1 and 2 which are horizontally split (this already works, we have a drag & drop mechanism for this already); it also works recursively.

what i want different: i dont drag and drop the current tabs into the canvas (currently the case, really confusing), but isntead i drag and drop into the canvas from the primary sidebar. i.e. tabs are only dragged and dropped for the tab order, not to move something into the canvas.

i can create a new composition by clicking on plus and it asks me to then drag the first element from the primary left sidebar, e.g. a floorplan.

then also i want that the icon of the view type (e.g. floorplan) can be used to toggle the secondary left sidebar of the specific view; this way, i don't need to have the "active" child in the canvas, but i can decide what i want to view whereever; each "tab" of a composition should show its own ribbon and such.

please start implementing this

---

[Image #1] no you didnt really get the goal done. please look into it further. what currently are the tabs will be the compositions (can stay in the same styling etc, but the current tab must disapper);
then i can easily open new compositions (really they must replace the current tab line, but should have the same styling); and then i can drag and drop from the primary left sidebar into the canvas and recusrively split

---

[Image #1]also not yet what i wanted, the secondary left sidebar must move into each children ("tab"), i.e. i must have it in the image twice; also the ribbon. and then i can toggle the respecitve secodnary sidebar of each tab off/on via the icon of the view type in the tab.

---

[Image #1] the element sidebar then of course must also be per tab. also, i didnt mean that the icon toggle is on the one on the name next to the tab but in the ribbon

---

i want that the discipline lens is per tab level, e.g. i could have e.g. the floorplan side by side for architect and structure or such

---

the disciple lens selection should be happening next to the icon of the view type of the respective tab (which also toggles the secondary left sidebar); so remove that from the primary left sidebar

---

please move view-type specific content from the footer into the secondary left sidebar; i only want global information there

---

did you commit and push and merge your changes? please do so if not yet