# Wave 6 — Coordination + Export

## Goal

Selection Sets, Find Items by Rule, Clash Detection, and Viewpoints with saved visibility state.

## Execution

Single WP — no parallel batching needed.

```
WP-V2-13    feat/wp-v2-13-coordination
```

## Done rule

(a) `pnpm exec tsc --noEmit` clean; (b) all new logic has vitest unit coverage; (c) `make verify` passes; (d) merged to main and pushed.
