# Wave 33 — WP-A: Complete Remaining Ribbon Tabs (§1.6.5)

You are an agent working on the **bim-ai** repo (`/Users/jhoetter/repos/bim-ai`).
bim-ai is a browser-based BIM authoring tool (React + TypeScript + Three.js, Vite, Vitest).

You are not alone in the codebase. Other agents may be editing adjacent Wave 33 files. Do not revert changes you did not make; adapt to them.

## Context

Wave 32 completed Steel, Precast, and Massing & Site ribbon tabs. The only remaining explicit P1 partial in the tracker is §1.6.5 "Multifunktionsleiste". The current ribbon already has many tabs in `packages/web/src/workspace/shell/RibbonBar.tsx`, but §1.6.5 still calls out incomplete MEP/System, Insert, Calculate/Analyze, Collaborate, View, Manage, and Modify ribbon coverage.

Your task is to make the ribbon surface feel complete without inventing new engines. Use existing tools/actions only.

## Ownership

Primary write scope:
- `packages/web/src/workspace/shell/RibbonBar.tsx`
- Add one focused test if needed under `packages/web/src/workspace/shell/`

Avoid editing command capability metadata unless a compile/test failure requires it; WP-B owns that.

## Orientation

Run:
- `rg -n "type RibbonTabId|type RibbonActionId|function buildPlanRibbonTabs|function build3dRibbonTabs|function build.*ModifyTab|function tool|function action" packages/web/src/workspace/shell/RibbonBar.tsx`
- `rg -n "id: '(systems|insert|annotate|analyze|collaborate|view|manage|modify|review)'" packages/web/src/workspace/shell/RibbonBar.tsx`
- `rg -n "export type ToolId|id: '(duct|pipe|cable-tray|mep-equipment|fixture|mep-terminal|dimension|tag|measure|section|elevation|grid|reference-plane|move|copy|rotate|array|align|trim|offset|mirror)'" packages/web/src/tools/toolRegistry.ts`

## Required Work

1. In `buildPlanRibbonTabs`, normalize remaining Revit-style tab IDs:
   - Keep existing `systems` tab but make it clearly cover MEP: routes, devices, coordination.
   - Add an `analyze` tab if not present, using existing actions/tools such as advisor/checks, measure, route/clearance analysis only if already represented by existing actions or tools.
   - Add `collaborate` tab using existing project links/manage links/advisor actions.
   - Split or strengthen `view` and `manage` tabs if they are missing in plan mode. Use existing actions: settings, visibility graphics, phases, global parameters, dimension style, command palette.
   - Preserve existing `insert`, `annotate`, `steel`, `precast`, `massing-site`, and contextual `modify` behavior.

2. In `build3dRibbonTabs`, add comparable `collaborate`, `manage`, and `massing-site`/systems coverage where existing 3D-capable tools/actions support it.

3. Do not add fake disabled commands. `ribbonCommandReachabilityForMode()` has tests that reject disabled commands in active schemas.

4. Prefer stable test IDs generated by the existing `tool()` and `action()` helpers. If you add action commands that need explicit test IDs, use names like `ribbon-command-manage-links`, not prose.

5. Add/update a focused test that renders the plan ribbon and confirms the new tabs are openable.

## Acceptance

- Existing `TopBar.test.tsx` still passes.
- No invalid `ToolId`, `IconName`, `RibbonActionId`, or tab ID is introduced.
- `pnpm --filter @bim-ai/web exec vitest run src/workspace/shell/TopBar.test.tsx` passes.

