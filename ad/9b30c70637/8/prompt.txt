You are not alone in the codebase; do not revert edits made by others, and adjust to accommodate parallel changes. Task: implement a backend constructability collision proxy module and focused tests. Own only new files under app/bim_ai/constructability_geometry.py and app/tests/test_constructability_geometry.py unless absolutely necessary. Requirements: expose dataclasses/functions to build deterministic AABB/OBB-ish collision participants from existing elements for at least wall, floor, roof conservative footprint, stair conservative run envelope, column, beam, pipe, duct, placed_asset, family_instance. Keep it pragmatic: AABB with element kind/category/discipline/level/metadata is acceptable for v0, but account for rotation for box-like assets by returning a wrapping AABB. Include overlap/distance helpers and a function collect_physical_participants(elements: dict[str, Element]) -> list[PhysicalParticipant]. Include unsupported diagnostics for physical kinds that cannot produce proxies if relevant. Tests should prove: wall AABB dimensions, placed_asset/family_instance size inferred from param values/family type params, wall+shelf overlap candidate, non-overlap distance, pipe/duct AABBs. Do not modify constraints_evaluation.py; main agent will integrate.