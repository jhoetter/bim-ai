You are Worker for creating `target-house-3` as an ACCEPTED sketch-to-BIM seed artifact in `/Users/jhoetter/repos/bim-ai`. Read this carefully: the deliverable is not files. The deliverable is a zero-warning accepted seed with evidence. Do not stop at a draft.

You are not alone in the codebase. There are unrelated dirty files and concurrent commits. Do not revert or modify unrelated files. Your write ownership is limited to `seed-artifacts/target-house-3/**` and any narrowly necessary generated evidence for that artifact. If you think a shared script/test/backend rule must change, stop and report `blocked`; do not edit shared files.

Required reading before edits:
- `claude-skills/sketch-to-bim/SKILL.md` especially Completion status semantics.
- `spec/sketch-to-bim-methodology.md` especially Completion Status Gate.
- `spec/sketch-to-bim-process-audit-tracker.md` especially Worker Instruction Template.
- `spec/seed-artifacts.md`.
- `spec/target-house/target-house-seed.md`.
- Inspect images in `spec/target-house/`; use `target-house-2.png` and `floorplan.png` as the primary visual/spec target for this attempt unless the image evidence clearly says otherwise.
- You may inspect `seed-artifacts/target-house-1` and `seed-artifacts/target-house-2` as references, but do not overwrite them and do not clone their unresolved warning state.

Task:
Create `seed-artifacts/target-house-3` as a new accepted seed attempt for the target house. This is a quality pass after target-house-2 failed acceptance with door/window clearance, furniture/asset clashes, physical clashes, and room access warnings. Your main goal is to model the same target-house spec while eliminating those warnings.

Required deliverables:
- `seed-artifacts/target-house-3/manifest.json`
- `seed-artifacts/target-house-3/bundle.json`
- `seed-artifacts/target-house-3/source/` with portable copies from `spec/target-house/`
- `seed-artifacts/target-house-3/evidence/target-house-3.recipe.json`
- `seed-artifacts/target-house-3/evidence/sketch-ir.json`
- `seed-artifacts/target-house-3/evidence/assumptions.md`
- `seed-artifacts/target-house-3/evidence/status.md`
- `seed-artifacts/target-house-3/evidence/material-check.json`
- phase packet folders with files required by `sketch_bim.py phase-accept` where feasible
- final live evidence from `sketch_bim.py accept` if the live API/web tooling is available

Mandatory status semantics:
- `accepted`: only if strict final acceptance passes at current HEAD with zero Advisor warning/error findings.
- `draft`: files compile/load but any warning/error, visual issue, missing evidence, stale evidence, or incomplete phase packet remains.
- `blocked`: you cannot continue because of a concrete external/tooling blocker such as API/web unavailable, dependency missing, verifier defect, or app crash.

Do not report done/complete/final/accepted for a draft. If warnings appear, fix the source recipe and rerun. Do not merely document warnings in `status.md`. No warning tolerance is granted by default.

Quality bar:
- Produce a reviewable `seed-dsl.v0` recipe, not only raw command blocks.
- Use first-class DSL support for levels/types/volumes/roofs/rooms/viewpoints/assets/placedAssets/materialIntent/materialAssignments/features where appropriate.
- Include interior programme assets, but place them with enough clearance so they do not create `furniture_wall_hard_clash`, `physical_hard_clash`, door conflicts, or window conflicts.
- Include material intent: white wrapper, ground cladding/base material, glass, railings/terrace treatment as visible in the target.
- Prefer typed walls/floors/roofs/openings/railings/assets. No final `createMass` placeholders.
- Avoid universal fake room-separation rectangles and metadata-only roof openings.
- Fix or avoid the known target-house-2 warning classes: `door_operation_clearance_conflict`, `window_operation_clearance_conflict`, `furniture_wall_hard_clash`, `physical_hard_clash`, `room_without_door_access`.

Required command loop:
1. `python3 claude-skills/sketch-to-bim/sketch_bim.py archetypes --query "modern two storey"`
2. Author `target-house-3.recipe.json` and `sketch-ir.json`.
3. `python3 claude-skills/sketch-to-bim/sketch_bim.py compile --seed target-house-3`
4. `python3 claude-skills/sketch-to-bim/sketch_bim.py material-check --seed target-house-3 --out seed-artifacts/target-house-3/evidence/material-check.json --fail-on-missing`
5. `python3 claude-skills/sketch-to-bim/sketch_bim.py seed --seed target-house-3 --clear`
6. `python3 claude-skills/sketch-to-bim/sketch_bim.py advisor --model <model-id> --out seed-artifacts/target-house-3/evidence/phase-<n> --fail-on-warning`
7. If warnings exist, edit the source recipe and repeat from compile. Do not stop.
8. When CLI Advisor has zero warning/error findings, run:
   `python3 claude-skills/sketch-to-bim/sketch_bim.py accept --seed target-house-3 --clear`
9. Run `node scripts/verify-sketch-seed-artifacts.mjs --seed target-house-3`.
10. If web app is available, capture `browser-evidence` and `advisor-parity`. If web is unavailable but API acceptance passes, document browser evidence as unavailable and explain exact failing command.

Commit requirements:
- Commit your focused artifact work only if possible, e.g. `Add accepted target-house-3 seed artifact` if accepted, or `Add draft target-house-3 seed artifact` only if truly blocked and useful.
- Do not stage unrelated dirty files.

Final response back to parent:
- State status exactly: `accepted`, `draft`, or `blocked`.
- List exact files changed.
- List commands run and results.
- If not `accepted`, list exact remaining blocker commands/findings. If Advisor warnings remain, do not imply success.