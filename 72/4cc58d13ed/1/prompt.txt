please remove the ones that are already fully covered..

---

wave 6 is now also done. delete buckets 1 and 2. for bucket 3, i will look into next

---

what about files like @spec/rendering-v2-spec.md ? look into the other md files, which of those also are completed?

---

write a prompt that i can copy-paste to another subagent which will implement missing parts of FL-06, FL-08, R5 and R7.

---

ok, interesting - what about the other files, e.g. @spec/ifc-export-wp-x03-slice.md and such?

---

can you please consolidate overall these points into one clean tracker with open items outstanding and then remove the old files? i am losing the overview

---

even now i am thinking: is it possible to consolidate basically everything into that single workpackage-master-tracker that is outstanding? the house seed has a description of a house, that is something different - that you can keep. but the other things: can you analyze them whether they are already completed (if yes delete them), if not merge them literally into the workpackage master tracker? i want a single point of truth document that is self-containing

---

is the @spec/prd/revit-production-parity-ai-agent-prd.md still needed, or also fully completed? or is it a good reference for what was implemented? but then it is outdated? and what are PRD tests? do we run automated tests against the PRD? is that even needed?

---

yeah that sounds very good, absolutely reasonable. go ahead