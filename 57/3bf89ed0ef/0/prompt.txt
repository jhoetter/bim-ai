You are implementing GW-01 in a BIM web app (monorepo at /Users/jhoetter/repos/bim-ai).

  ## Goal
  Add `isCurtainWall?: boolean` to the wall element type. The field `roofAttachmentId?: string | null`
  already exists in `core/src/index.ts` (line ~175) and is already coerced in
  `packages/web/src/state/store.ts` (line ~338), so do NOT touch those.

  ## Task — exactly two file edits

  ### 1. `packages/core/src/index.ts`
  Find the `kind: 'wall'` object inside the `Element` union (around line 161–177).
  Add one line after `insulationExtensionMm?: number;`:

    isCurtainWall?: boolean;

  ### 2. `packages/web/src/state/store.ts`
  Find the wall coercion block (around line 310–342).
  After the `insulationExtensionMm` line add:

    ...(raw.isCurtainWall != null || raw.is_curtain_wall != null
      ? { isCurtainWall: Boolean(raw.isCurtainWall ?? raw.is_curtain_wall) }
      : {}),

  ## Verification
  Run from repo root:
    pnpm --filter @bim-ai/core build
    pnpm --filter @bim-ai/web typecheck

  Both must exit 0 with no errors. No other files should be changed.

  ## Branch & commit
  Branch: gw-01-wall-curtain-field
  Commit message: "feat(model): add isCurtainWall field to wall element"
  Push to origin.